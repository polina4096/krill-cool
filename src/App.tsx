import logo from "./assets/logo.gif";

export function App() {
  return (
    <div class="h-screen flex items-center justify-center">
      <img src={logo} class="w-[75%] max-w-screen-lg" />
    </div>
  );
}
